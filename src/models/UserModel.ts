class HomePage {
    constructor(parameters) {
        
    }
    this.header: string;

    doLogin(username: string, password: string): boolean {
        return true;
    }

    doResetPassowrd(oldpass, newpass): void {
        
    }
}